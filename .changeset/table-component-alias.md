---
"@ioi-dev/vue-table": patch
---

Promote `Table` as the preferred component export name while keeping `IoiTable` and `DataTable` as backward-compatible aliases.

Documentation and playground examples now use `Table`.
