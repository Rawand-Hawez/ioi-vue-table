---
"@ioi-dev/vue-table": patch
---

Add JS-first CSV import preview + commit APIs with delimiter autodetect, case-insensitive header mapping, preview row limits, per-row validation errors, nested-path writes, and JSON-array cell parsing fallback behavior.
